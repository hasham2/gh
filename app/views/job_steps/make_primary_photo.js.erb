/*Reloading primary photo*/
$("#primary_image").children().remove();
$("<%= escape_javascript(image_tag @primary_photo.first.image.url(:medium)) %>").appendTo("#primary_image");





/*Reloading additional photos*/
$(".additional-photos").children().remove();
$(".additional-photos").append("<%= j render(@photos) %>");